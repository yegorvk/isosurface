[package]
name = "demo"
version = "0.1.0"
edition = "2021"

[dependencies]
isosurface = { path = "../isosurface" }
bevy = { version = "0.15.0", features = ["wayland"] }
bevy_flycam = "0.15.0"
noise = "0.9.0"